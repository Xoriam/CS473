var main = function () {
	"use strict";

	/*This is actually just one string,
	  but I spread it out over two lines
	  to make it readable*/
	  
	console.log("Hello World!");
};

$(document).ready(main);